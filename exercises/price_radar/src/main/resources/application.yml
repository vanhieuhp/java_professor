spring:
  application:
    name: price_radar
  data:
    redis:
      host: localhost
      port: 6379
  datasource:
    url: jdbc:postgresql://localhost:5432/priceradar
    username: admin
    password: password
  jpa:
    hibernate:
      ddl-auto: update    # auto-creates tables (dev only!)
    show-sql: true